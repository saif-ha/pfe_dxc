import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dxc mobile app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
